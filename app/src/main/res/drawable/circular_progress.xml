<?xml version="1.0" encoding="utf-8"?>
<animated-rotate
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:fromDegrees="0"
    android:toDegrees="360"
    android:pivotX="50%"
    android:pivotY="50%"
    android:interpolator="@android:anim/linear_interpolator">

    <!-- 2026 Fluid Ring Progress -->
    <shape
        android:shape="ring"
        android:thickness="@dimen/stroke_ring_thickness"
        android:useLevel="false"
        android:innerRadiusRatio="3">

        <size
            android:width="@dimen/progress_ring_size"
            android:height="@dimen/progress_ring_size"/>

        <gradient
            android:type="sweep"
            android:useLevel="false"
            android:startColor="@color/primary_color"
            android:centerColor="@color/primary_light_color"
            android:endColor="@color/colorAccent"
            android:angle="0"/>
    </shape>
</animated-rotate>